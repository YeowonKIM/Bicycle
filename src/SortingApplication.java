import controller.SortingController;

/**
 * Running processes to assess the execution time of each sorting algorithm.
 */
public class SortingApplication {
    public static void main(String[] args) {
        SortingController sortingController = new SortingController();
        sortingController.runSortingNavigator();
    }
}
